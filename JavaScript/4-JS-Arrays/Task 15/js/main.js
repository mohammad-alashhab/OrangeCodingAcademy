//Use the for...in loop to iterate through the indexes of an array.

let fruits = ['apple', 'banana', 'cherry', 'date'];

for (let i in fruits) {
    console.log('Index:', i);
    console.log('Value:', fruits[i]);
}
