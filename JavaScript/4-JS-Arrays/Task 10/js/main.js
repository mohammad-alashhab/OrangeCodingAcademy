//Use the indexOf() method to find the index of a specific element in an array.

const fruits = ['apple', 'banana', 'cherry', 'date'];

const indexOfBanana = fruits.indexOf('cherry');

console.log(indexOfBanana);
