//Use the Object.keys() method to get an array of an object's properties. 

const obj = { 
    name: 'John', 
    age: 30, 
    city: 'New York' 
};

const properties = Object.keys(obj);

console.log(properties);
