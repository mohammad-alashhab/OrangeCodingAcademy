// Create an object with properties name, age, and gender and print its values to the console. 

    const person = {
        name: "John Doe",
        age: 30,
        gender: "Male"
    };
    
    console.log(`${person.name} ${person.age} ${person.gender}`);