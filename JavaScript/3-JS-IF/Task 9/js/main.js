//-Check if a variable named "y" is a negative number and display a message "y is a negative number" if it is. 

    let y = -5;

    if (y < 0) {
        console.log("y is a negative number");
    }else
    console.log("y is not a negative number");