//-Check if a variable named "day" is not equal to "Saturday" and "Sunday" and display a message "It's a weekday" if it is. 

    let day = "Monday";

    if (day !== "Saturday" && day !== "Sunday") {
        console.log("It's a weekday");
    }